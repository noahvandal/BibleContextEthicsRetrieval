# Embedding

Based on compute constraints, and general tradeoffs, the model selected to be ideal as of April 4, 2026, is:
`Qwen3-embedding-4B-GGUF` 8-bit version.

We can just use it with the llama.cpp server running.